{{outlet}}
<div>


</div>
<form  {{action 'login' on="submit"}} align="center">
    <div class="control-group">
        <div class="control-label">
            <label>Email</label>
        </div>

        <div class="controls">
          {{input value=email type="text" class="form-control "  placeholder="Email"  required=true }}
        </div>
    </div>

    <div >
        <div class="control-label">
            <label>Password</label>
        </div>

        <div class="controls">
          {{input value=password type="password" class="form-control "  placeholder="Password" required=true }}
        </div>
    </div>

  <br>

    <button type="submit" class="btn">Log in!</button>
</form>